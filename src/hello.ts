/**
 * 返回 hello 开头的字符串
 * @param str - input string
 * @returns 'hello xxx'
 * @example
 * ```ts
 * hello('ts') => 'hello ts'
 * ```
 *
 * @beta
 */
export const hello = (str: string) => `hello ${str}`
